# Vedic Astrology skill — Python dependencies
# Install with: pip install -r requirements.txt

pyswisseph>=2.10.3   # Swiss Ephemeris binding (AGPL-3.0 or commercial) — planetary positions
pytz>=2024.1         # IANA timezone database for local->UT conversion
